#include <stdio.h>

int main(void);

int(m);
int(n);

(
    // The numbers have to be equal
    if n = m
    printf("%i, \n")

    // Number n have to be greater than m
    else n > m;
    printf("%i , \n")

    // Number m have to be greater than n
    else if n < m;
    printf("%i , \n")
)
